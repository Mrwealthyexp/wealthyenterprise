events {}

http {
  upstream web_upstream {
    server web:3000;
  }

  upstream api_upstream {
    server api:4000;
  }

  server {
    listen 80;

    location /api/ {
      proxy_pass http://web_upstream;
      proxy_set_header Host $host;
    }

    location / {
      proxy_pass http://web_upstream;
      proxy_set_header Host $host;
    }
  }
}
